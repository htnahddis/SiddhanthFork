/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','assets.aceternity.com','media2.giphy.com'],
      },
};

export default nextConfig;
